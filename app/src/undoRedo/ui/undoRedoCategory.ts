import {createStandardCategory} from "../../menus/categories/createStandardCategory";

/**
 * The category for undo and redo menus
 */
export const undoRedoCategory = createStandardCategory({name: "Edit"});
