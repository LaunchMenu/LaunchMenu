import {FC} from "react";
import {ICloseTransitionProps} from "./ICloseTransitionProps";

/**
 * A transition component for closing components
 */
export type ICloseTransition = FC<ICloseTransitionProps>;
