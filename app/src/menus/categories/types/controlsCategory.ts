import {createStandardCategory} from "../createStandardCategory";

/**
 * A category to put the controls of a menu in (in case the rest of the items are values)
 */
export const controlsCategory = createStandardCategory({name: "Controls"});
