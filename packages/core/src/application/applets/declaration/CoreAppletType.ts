/**
 * Categories for core applets
 */
export enum CoreAppletType {
    SETTINGS = "settings",
    APPLETS = "applets",
    HELP = "help",
    SESSIONS = "sessions",
    WINDOW = "window",
    STYLING = "styling",
    UNDO = "undo",
}
